const server = require('./index.js');

describe('index.js', () => {
    it('should run the tests', () => {
        expect(true).toBeTruthy();
    });

    it('should run other tests', () => {
        expect(true).toBeTruthy();
    });
});